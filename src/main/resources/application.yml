spring:
  application:
    name: PProject API
  datasource:
    url: jdbc:h2:mem:db
    username: sa
    password: sa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console

